import { RamdonLetterPipe } from './ramdon-letter.pipe';

describe('RamdonLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new RamdonLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
